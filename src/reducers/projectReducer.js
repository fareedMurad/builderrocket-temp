import {  } from '../actions/types';

const intialState = {
    project: {}
};

const projectReducer = (state = intialState, action) => {
    switch (action.type) {
     
        default:
            return state;
    }
}

export default projectReducer;