import {

} from '../actions/types';

const intialState = {
    subdivision: {}
}

const subdivisionReducer = (state = intialState, action) => {
    switch (action.type) {
        default: 
            return state;
    }
};

export default subdivisionReducer;